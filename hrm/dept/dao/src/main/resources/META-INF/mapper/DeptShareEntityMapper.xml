<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cii.bomse.hrm.dept.entity.DeptShareEntity">

    <resultMap id="BaseResultMap" type="com.cii.bomse.hrm.dept.entity.DeptShareEntity">
        <result column="dept_from_id" property="deptFromId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="dept_from_name" property="deptFromName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="dept_to_id" property="deptToId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="dept_to_name" property="deptToName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="share_state" property="shareState" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="id" property="id" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="remark" property="remark" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="create_id" property="createId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="create_name" property="createName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="create_dept_id" property="createDeptId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="create_dept_name" property="createDeptName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" javaType="java.util.Date"/>
        <result column="cpy_id" property="cpyId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
    </resultMap>

    <!-- 批量新增信息到数据库中 -->
    <insert id="batchInsert" parameterType="java.util.List">
        <foreach collection="list" item="i" separator=";">
            INSERT INTO t_boms_hrm_dept_dept_share
            <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="i.deptFromId != null">
                    dept_from_id,
                </if>
                <if test="i.deptFromName != null and i.deptFromName != ''">
                    dept_from_name,
                </if>
                <if test="i.deptToId != null">
                    dept_to_id,
                </if>
                <if test="i.deptToName != null and i.deptToName != ''">
                    dept_to_name,
                </if>
                <if test="i.shareState != null and i.shareState != ''">
                    share_state,
                </if>
                <if test="i.id != null">
                    id,
                </if>
                <if test="i.remark != null and i.remark != ''">
                    remark,
                </if>
                <if test="i.createId != null">
                    create_id,
                </if>
                <if test="i.createName != null and i.createName != ''">
                    create_name,
                </if>
                <if test="i.createDeptId != null">
                    create_dept_id,
                </if>
                <if test="i.createDeptName != null and i.createDeptName != ''">
                    create_dept_name,
                </if>
                <if test="i.createTime != null">
                    create_time,
                </if>
                <if test="i.cpyId != null">
                    cpy_id,
                </if>

            </trim>
            <trim prefix="values (" suffix=")" suffixOverrides=",">

                <if test="i.deptFromId != null">
                    #{i.deptFromId},
                </if>
                <if test="i.deptFromName != null and i.deptFromName != ''">
                    #{i.deptFromName},
                </if>
                <if test="i.deptToId != null">
                    #{i.deptToId},
                </if>
                <if test="i.deptToName != null and i.deptToName != ''">
                    #{i.deptToName},
                </if>
                <if test="i.shareState != null and i.shareState != ''">
                    #{i.shareState},
                </if>
                <if test="i.id != null">
                    #{i.id},
                </if>
                <if test="i.remark != null and i.remark != ''">
                    #{i.remark},
                </if>
                <if test="i.createId != null">
                    #{i.createId},
                </if>
                <if test="i.createName != null and i.createName != ''">
                    #{i.createName},
                </if>
                <if test="i.createDeptId != null">
                    #{i.createDeptId},
                </if>
                <if test="i.createDeptName != null and i.createDeptName != ''">
                    #{i.createDeptName},
                </if>
                <if test="i.createTime != null">
                    #{i.createTime},
                </if>
                <if test="i.cpyId != null">
                    #{i.cpyId},
                </if>
            </trim>
        </foreach>
    </insert>

    <!-- 批量更新数据信息 -->
    <update id="batchUpdate" parameterType="java.util.List">
        <foreach collection="list" item="i" index="index" separator=";">
            UPDATE t_boms_hrm_dept_dept_share
            <trim prefix="set" suffixOverrides=",">
                <if test="i.deptFromId != null">
                    dept_from_id = #{i.deptFromId},
                </if>
                <if test="i.deptFromName != null and i.deptFromName != ''">
                    dept_from_name = #{i.deptFromName},
                </if>
                <if test="i.deptToId != null">
                    dept_to_id = #{i.deptToId},
                </if>
                <if test="i.deptToName != null and i.deptToName != ''">
                    dept_to_name = #{i.deptToName},
                </if>
                <if test="i.shareState != null and i.shareState != ''">
                    share_state = #{i.shareState},
                </if>
                <if test="i.id != null">
                    id = #{i.id},
                </if>
                <if test="i.remark != null and i.remark != ''">
                    remark = #{i.remark},
                </if>
                <if test="i.createId != null">
                    create_id = #{i.createId},
                </if>
                <if test="i.createName != null and i.createName != ''">
                    create_name = #{i.createName},
                </if>
                <if test="i.createDeptId != null">
                    create_dept_id = #{i.createDeptId},
                </if>
                <if test="i.createDeptName != null and i.createDeptName != ''">
                    create_dept_name = #{i.createDeptName},
                </if>
                <if test="i.createTime != null">
                    create_time = #{i.createTime},
                </if>
                <if test="i.cpyId != null">
                    cpy_id = #{i.cpyId},
                </if>
            </trim>
            <trim prefix="where" prefixOverrides="AND|OR">
                id = #{i.id}
                <if test="i.cpyId != null">
                    AND cpy_id = #{i.cpyId}
                </if>
            </trim>
        </foreach>
    </update>

    <!-- 通过主键批量物理删除数据 -->
    <delete id="batchDeleteByIds" parameterType="java.util.List">
        DELETE FROM t_boms_hrm_dept_dept_share WHERE ID IN
        <foreach collection="list" item="i" open="(" separator="," close=")">
            #{i}
        </foreach>
    </delete>

    <!-- 通过条件删除指定数据-->
    <delete id="deleteByMap" parameterType="map">
        delete from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"></include>
    </delete>
    <!--执行本地更新 -->
    <update id="updateByNativeSql" parameterType="string">
        ${value}
    </update>

    <!-- 根据原生Sql查询记录 -->
    <select id="selectByNativeSql" parameterType="string" resultMap="BaseResultMap">
        ${value}
    </select>

    <!-- 根据原生SQL查询单条记录-->
    <select id="selectOneByNativeSql" parameterType="string" resultMap="BaseResultMap">
        ${value}
    </select>

    <!-- 根据原生Sql统计数量 -->
    <select id="countByNativeSql" parameterType="string" resultType="int">
        ${value}
    </select>

    <!-- 通用脚本查询条件 -->
    <sql id="selectSql">
        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="keyword != null">
                AND (
                t.dept_from_name like '%'||#{keyword}||'%'
                or t.dept_to_name like '%'||#{keyword}||'%'
                )
            </if>
            <if test="deptId != null">
                AND (
                t.dept_from_id = #{deptId}
                or t.dept_to_id = #{deptId}
                )
            </if>

            <if test="deptFromId != null">
                AND t.dept_from_id = #{deptFromId}
            </if>
            <if test="deptFromName != null">
                AND t.dept_from_name = #{deptFromName}
            </if>
            <if test="deptToId != null">
                AND t.dept_to_id = #{deptToId}
            </if>
            <if test="deptToName != null">
                AND t.dept_to_name = #{deptToName}
            </if>
            <if test="shareState != null">
                AND t.share_state = #{shareState}
            </if>
            <if test="id != null">
                AND t.id = #{id}
            </if>
            <if test="remark != null">
                AND t.remark = #{remark}
            </if>
            <if test="createId != null">
                AND t.create_id = #{createId}
            </if>
            <if test="createName != null">
                AND t.create_name = #{createName}
            </if>
            <if test="createDeptId != null">
                AND t.create_dept_id = #{createDeptId}
            </if>
            <if test="createDeptName != null">
                AND t.create_dept_name = #{createDeptName}
            </if>
            <if test="createTimeStart != null">
                AND t.create_time <![CDATA[ >= ]]> #{createTimeStart}
            </if>
            <if test="createTimeEnd != null">
                AND t.create_time <![CDATA[ <= ]]> #{createTimeEnd}
            </if>
            <if test="cpyId != null">
                AND t.cpy_id = #{cpyId}
            </if>
        </trim>
    </sql>

    <!-- 查询唯一的信息 -->
    <select id="selectUniqueByProp" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT t.* from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
    </select>

    <!--查询字段-->
    <select id="selectUniqueByPropColumns" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        <choose>
            <when test="_columns != null">
                <foreach collection="_columns" separator="," item="item">
                    ${item}
                </foreach>
            </when>
            <otherwise>
                t.*
            </otherwise>
        </choose>
        from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="countByMap" parameterType="java.util.Map" resultType="int">
        SELECT count(*) FROM t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息 -->
    <select id="selectByMap" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT t.* from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
        <if test="orderBy != null">
            ORDER BY ${orderBy}
        </if>
        <if test="order != null">
            ${order}
        </if>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="selectByMapCount" parameterType="java.util.Map" resultType="int">
        SELECT count(*) from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息 -->
    <select id="selectByMapSimple" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        <choose>
            <when test="_columns != null">
                <foreach collection="_columns" separator="," item="item">
                    ${item}
                </foreach>
            </when>
            <otherwise>
                t.*
            </otherwise>
        </choose>
        from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
        <if test="orderBy != null">
            ORDER BY ${orderBy}
        </if>
        <if test="order != null">
            ${order}
        </if>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="selectByMapSimpleCount" parameterType="java.util.Map" resultType="int">
        SELECT count(*) from t_boms_hrm_dept_dept_share t
        <include refid="selectSql"/>
    </select>
</mapper>