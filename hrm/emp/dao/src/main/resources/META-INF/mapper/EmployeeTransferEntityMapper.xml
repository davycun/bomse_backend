<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cii.bomse.hrm.emp.entity.EmployeeTransferEntity">

    <resultMap id="BaseResultMap" type="com.cii.bomse.hrm.emp.entity.EmployeeTransferEntity">
        <result column="emp_id" property="empId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="emp_name" property="empName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="transfer_date" property="transferDate" jdbcType="TIMESTAMP" javaType="java.util.Date"/>
        <result column="from_dept_id" property="fromDeptId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="from_dept_name" property="fromDeptName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="to_dept_id" property="toDeptId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="to_dept_name" property="toDeptName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="id" property="id" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="remark" property="remark" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="create_id" property="createId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" javaType="java.util.Date"/>
        <result column="cpy_id" property="cpyId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
    </resultMap>

    <!-- 批量新增信息到数据库中 -->
    <insert id="batchInsert" parameterType="java.util.List">
        <foreach collection="list" item="i" separator=";">
            INSERT INTO t_boms_hrm_emp_transfer
            <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="i.empId != null">
                    emp_id,
                </if>
                <if test="i.empName != null and i.empName != ''">
                    emp_name,
                </if>
                <if test="i.transferDate != null">
                    transfer_date,
                </if>
                <if test="i.fromDeptId != null">
                    from_dept_id,
                </if>
                <if test="i.fromDeptName != null and i.fromDeptName != ''">
                    from_dept_name,
                </if>
                <if test="i.toDeptId != null">
                    to_dept_id,
                </if>
                <if test="i.toDeptName != null and i.toDeptName != ''">
                    to_dept_name,
                </if>
                <if test="i.id != null">
                    id,
                </if>
                <if test="i.remark != null and i.remark != ''">
                    remark,
                </if>
                <if test="i.createId != null">
                    create_id,
                </if>
                <if test="i.createTime != null">
                    create_time,
                </if>
                <if test="i.cpyId != null">
                    cpy_id,
                </if>

            </trim>
            <trim prefix="values (" suffix=")" suffixOverrides=",">

                <if test="i.empId != null">
                    #{i.empId},
                </if>
                <if test="i.empName != null and i.empName != ''">
                    #{i.empName},
                </if>
                <if test="i.transferDate != null">
                    #{i.transferDate},
                </if>
                <if test="i.fromDeptId != null">
                    #{i.fromDeptId},
                </if>
                <if test="i.fromDeptName != null and i.fromDeptName != ''">
                    #{i.fromDeptName},
                </if>
                <if test="i.toDeptId != null">
                    #{i.toDeptId},
                </if>
                <if test="i.toDeptName != null and i.toDeptName != ''">
                    #{i.toDeptName},
                </if>
                <if test="i.id != null">
                    #{i.id},
                </if>
                <if test="i.remark != null and i.remark != ''">
                    #{i.remark},
                </if>
                <if test="i.createId != null">
                    #{i.createId},
                </if>
                <if test="i.createTime != null">
                    #{i.createTime},
                </if>
                <if test="i.cpyId != null">
                    #{i.cpyId},
                </if>
            </trim>
        </foreach>
    </insert>

    <!-- 批量更新数据信息 -->
    <update id="batchUpdate" parameterType="java.util.List">
        <foreach collection="list" item="i" index="index" separator=";">
            UPDATE t_boms_hrm_emp_transfer
            <trim prefix="set" suffixOverrides=",">
                <if test="i.empId != null">
                    emp_id = #{i.empId},
                </if>
                <if test="i.empName != null and i.empName != ''">
                    emp_name = #{i.empName},
                </if>
                <if test="i.transferDate != null">
                    transfer_date = #{i.transferDate},
                </if>
                <if test="i.fromDeptId != null">
                    from_dept_id = #{i.fromDeptId},
                </if>
                <if test="i.fromDeptName != null and i.fromDeptName != ''">
                    from_dept_name = #{i.fromDeptName},
                </if>
                <if test="i.toDeptId != null">
                    to_dept_id = #{i.toDeptId},
                </if>
                <if test="i.toDeptName != null and i.toDeptName != ''">
                    to_dept_name = #{i.toDeptName},
                </if>
                <if test="i.id != null">
                    id = #{i.id},
                </if>
                <if test="i.remark != null and i.remark != ''">
                    remark = #{i.remark},
                </if>
                <if test="i.createId != null">
                    create_id = #{i.createId},
                </if>
                <if test="i.createTime != null">
                    create_time = #{i.createTime},
                </if>
                <if test="i.cpyId != null">
                    cpy_id = #{i.cpyId},
                </if>
            </trim>
            <trim prefix="where" prefixOverrides="AND|OR">
                id = #{i.id}
                <if test="i.cpyId != null">
                    AND cpy_id = #{i.cpyId}
                </if>
            </trim>
        </foreach>
    </update>

    <!-- 通过主键批量物理删除数据 -->
    <delete id="batchDeleteByIds" parameterType="java.util.List">
        DELETE FROM t_boms_hrm_emp_transfer WHERE ID IN
        <foreach collection="list" item="i" open="(" separator="," close=")">
            #{i}
        </foreach>
    </delete>

    <!-- 通过条件删除指定数据-->
    <delete id="deleteByMap" parameterType="map">
        delete from t_boms_hrm_emp_transfer t
        <include refid="selectSql"></include>
    </delete>
    <!--执行本地更新 -->
    <update id="updateByNativeSql" parameterType="string">
        ${value}
    </update>

    <!-- 根据原生Sql查询记录 -->
    <select id="selectByNativeSql" parameterType="string" resultMap="BaseResultMap">
        ${value}
    </select>

    <!-- 根据原生SQL查询单条记录-->
    <select id="selectOneByNativeSql" parameterType="string" resultMap="BaseResultMap">
        ${value}
    </select>

    <!-- 根据原生Sql统计数量 -->
    <select id="countByNativeSql" parameterType="string" resultType="int">
        ${value}
    </select>

    <!-- 通用脚本查询条件 -->
    <sql id="selectSql">
        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="empId != null">
                AND t.emp_id = #{empId}
            </if>
            <if test="empName != null">
                AND t.emp_name = #{empName}
            </if>
            <if test="transferDateStart != null">
                AND t.transfer_date <![CDATA[ >= ]]> #{transferDateStart}
            </if>
            <if test="transferDateEnd != null">
                AND t.transfer_date <![CDATA[ <= ]]> #{transferDateEnd}
            </if>
            <if test="fromDeptId != null">
                AND t.from_dept_id = #{fromDeptId}
            </if>
            <if test="fromDeptName != null">
                AND t.from_dept_name = #{fromDeptName}
            </if>
            <if test="toDeptId != null">
                AND t.to_dept_id = #{toDeptId}
            </if>
            <if test="toDeptName != null">
                AND t.to_dept_name = #{toDeptName}
            </if>
            <if test="id != null">
                AND t.id = #{id}
            </if>
            <if test="remark != null">
                AND t.remark = #{remark}
            </if>
            <if test="createId != null">
                AND t.create_id = #{createId}
            </if>
            <if test="createTimeStart != null">
                AND t.create_time <![CDATA[ >= ]]> #{createTimeStart}
            </if>
            <if test="createTimeEnd != null">
                AND t.create_time <![CDATA[ <= ]]> #{createTimeEnd}
            </if>
            <if test="cpyId != null">
                AND t.cpy_id = #{cpyId}
            </if>
        </trim>
    </sql>

    <!-- 查询唯一的信息 -->
    <select id="selectUniqueByProp" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT t.* from t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
    </select>

    <!--查询字段-->
    <select id="selectUniqueByPropColumns" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        <choose>
            <when test="_columns != null">
                <foreach collection="_columns" separator="," item="item">
                    ${item}
                </foreach>
            </when>
            <otherwise>
                t.*
            </otherwise>
        </choose>
        from t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="countByMap" parameterType="java.util.Map" resultType="int">
        SELECT count(*) FROM t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息 -->
    <select id="selectByMap" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT t.* from t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
        <if test="orderBy != null">
            ORDER BY ${orderBy}
        </if>
        <if test="order != null">
            ${order}
        </if>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="selectByMapCount" parameterType="java.util.Map" resultType="int">
        SELECT count(*) from t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息 -->
    <select id="selectByMapSimple" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        <choose>
            <when test="_columns != null">
                <foreach collection="_columns" separator="," item="item">
                    ${item}
                </foreach>
            </when>
            <otherwise>
                t.*
            </otherwise>
        </choose>
        from t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
        <if test="orderBy != null">
            ORDER BY ${orderBy}
        </if>
        <if test="order != null">
            ${order}
        </if>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="selectByMapSimpleCount" parameterType="java.util.Map" resultType="int">
        SELECT count(*) from t_boms_hrm_emp_transfer t
        <include refid="selectSql"/>
    </select>
</mapper>