<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cii.bomse.base.area.entity.AreaCpyEntity">

    <resultMap id="BaseResultMap" type="com.cii.bomse.base.area.entity.AreaCpyEntity">
        <result column="parent_id" property="parentId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="area_name" property="areaName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="short_name" property="shortName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="area_type" property="areaType" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="longitude" property="longitude" jdbcType="NUMERIC" javaType="java.lang.Double"/>
        <result column="latitude" property="latitude" jdbcType="NUMERIC" javaType="java.lang.Double"/>
        <result column="sort" property="sort" jdbcType="NUMERIC" javaType="java.lang.Integer"/>
        <result column="id" property="id" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="remark" property="remark" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="create_id" property="createId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="create_name" property="createName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="last_update_id" property="lastUpdateId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
        <result column="last_update_name" property="lastUpdateName" jdbcType="VARCHAR" javaType="java.lang.String"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" javaType="java.util.Date"/>
        <result column="last_update_time" property="lastUpdateTime" jdbcType="TIMESTAMP" javaType="java.util.Date"/>
        <result column="is_deleted" property="isDeleted" jdbcType="BOOLEAN" javaType="java.lang.Boolean"/>
        <result column="cpy_id" property="cpyId" jdbcType="NUMERIC" javaType="java.lang.Long"/>
    </resultMap>

    <!-- 批量新增信息到数据库中 -->
    <insert id="batchInsert" parameterType="java.util.List">
        <foreach collection="list" item="i" separator=";">
            INSERT INTO t_cii_area_company
            <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="i.id != null">
                    id,
                </if>
                <if test="i.parentId != null">
                    parent_id,
                </if>
                <if test="i.cpyId != null">
                    cpy_id,
                </if>

            </trim>
            <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="i.id != null">
                    #{i.id},
                </if>
                <if test="i.parentId != null">
                    #{i.parentId},
                </if>
                <if test="i.cpyId != null">
                    #{i.cpyId},
                </if>
            </trim>
        </foreach>
    </insert>

    <!-- 批量更新数据信息 -->
    <update id="batchUpdate" parameterType="java.util.List">
        <foreach collection="list" item="i" index="index" separator=";">
            UPDATE t_cii_area_company
            <trim prefix="set" suffixOverrides=",">
                <if test="i.id != null">
                    id = #{i.id},
                </if>
                <if test="i.parentId != null">
                    parent_id = #{i.parentId},
                </if>
                <if test="i.cpyId != null">
                    cpy_id = #{i.cpyId},
                </if>
            </trim>
            <trim prefix="where" prefixOverrides="AND|OR">
                id = #{i.id}
                <if test="i.cpyId != null">
                    AND cpy_id = #{i.cpyId}
                </if>
            </trim>
        </foreach>
    </update>

    <!-- 通过主键批量物理删除数据 -->
    <delete id="batchDeleteByIds" parameterType="java.util.List">
        DELETE FROM t_cii_area_company WHERE ID IN
        <foreach collection="list" item="i" open="(" separator="," close=")">
            #{i}
        </foreach>
    </delete>

    <!-- 通过条件删除指定数据-->
    <delete id="deleteByMap" parameterType="map">
        delete from t_cii_area_company c

        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="parentId != null">
                AND c.parent_id = #{parentId}
            </if>
            <if test="id != null">
                AND c.id = #{id}
            </if>
            <if test="cpyId != null">
                AND c.cpy_id = #{cpyId}
            </if>
        </trim>
    </delete>
    <!--执行本地更新 -->
    <update id="updateByNativeSql" parameterType="string">
        ${value}
    </update>

    <!-- 根据原生Sql查询记录 -->
    <select id="selectByNativeSql" parameterType="string" resultMap="BaseResultMap">
        ${value}
    </select>

    <!-- 根据原生SQL查询单条记录-->
    <select id="selectOneByNativeSql" parameterType="string" resultMap="BaseResultMap">
        ${value}
    </select>

    <!-- 根据原生Sql统计数量 -->
    <select id="countByNativeSql" parameterType="string" resultType="int">
        ${value}
    </select>

    <!-- 通用脚本查询条件 -->
    <sql id="selectSql">
        <trim prefix="where" prefixOverrides="AND|OR">
            <if test="keyword != null">
                (
                t.area_name like '%' || #{keyword} || '%'
                or t.area_type like '%' || #{keyword} || '%'
                )
            </if>
            <if test="notAreaType != null">
                AND t.area_type != #{notAreaType}
            </if>
            <if test="parentId != null">
                AND t.parent_id = #{parentId}
            </if>
            <if test="areaName != null">
                AND t.area_name = #{areaName}
            </if>
            <if test="shortName != null">
                AND t.short_name = #{shortName}
            </if>
            <if test="areaType != null">
                AND t.area_type = #{areaType}
            </if>
            <if test="areaTypes !=null">
                AND t.area_type in
                <foreach collection="areaTypes" item="at" open="(" close=")" separator=",">
                    #{at}
                </foreach>
            </if>
            <if test="notAreaTypes !=null">
                AND t.area_type not in
                <foreach collection="notAreaTypes" item="at" open="(" close=")" separator=",">
                    #{at}
                </foreach>
            </if>
            <if test="longitudeStart != null">
                AND t.longitude <![CDATA[ >= ]]> #{longitudeStart}
            </if>
            <if test="longitudeEnd != null">
                AND t.longitude <![CDATA[ <= ]]> #{longitudeEnd}
            </if>
            <if test="latitudeStart != null">
                AND t.latitude <![CDATA[ >= ]]> #{latitudeStart}
            </if>
            <if test="latitudeEnd != null">
                AND t.latitude <![CDATA[ <= ]]> #{latitudeEnd}
            </if>
            <if test="sortStart != null">
                AND t.sort <![CDATA[ >= ]]> #{sortStart}
            </if>
            <if test="sortEnd != null">
                AND t.sort <![CDATA[ <= ]]> #{sortEnd}
            </if>
            <if test="id != null">
                AND c.id = #{id}
            </if>
            <if test="remark != null">
                AND t.remark = #{remark}
            </if>
            <if test="createId != null">
                AND t.create_id = #{createId}
            </if>
            <if test="createName != null">
                AND t.create_name = #{createName}
            </if>
            <if test="lastUpdateId != null">
                AND t.last_update_id = #{lastUpdateId}
            </if>
            <if test="lastUpdateName != null">
                AND t.last_update_name = #{lastUpdateName}
            </if>
            <if test="createTimeStart != null">
                AND t.create_time <![CDATA[ >= ]]> #{createTimeStart}
            </if>
            <if test="createTimeEnd != null">
                AND t.create_time <![CDATA[ <= ]]> #{createTimeEnd}
            </if>
            <if test="lastUpdateTimeStart != null">
                AND t.last_update_time <![CDATA[ >= ]]> #{lastUpdateTimeStart}
            </if>
            <if test="lastUpdateTimeEnd != null">
                AND t.last_update_time <![CDATA[ <= ]]> #{lastUpdateTimeEnd}
            </if>
            <if test="isDeleted != null">
                AND t.is_deleted = #{isDeleted}
            </if>
            <if test="cpyId != null">
                AND c.cpy_id = #{cpyId}
            </if>
        </trim>
    </sql>

    <!-- 查询唯一的信息 -->
    <select id="selectUniqueByProp" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT t.* from t_cii_area_company c
        left join t_cii_area_area t on t.id = c.id
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="countByMap" parameterType="java.util.Map" resultType="int">
        SELECT count(c.id) FROM t_cii_area_company c
        left join t_cii_area_area t on t.id = c.id
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息 -->
    <select id="selectByMap" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        t.parent_id,
        t.area_name,
        t.short_name,
        t.area_type,
        t.longitude,
        t.latitude,
        t.sort,
        t.id,
        t.remark,
        t.is_deleted,
        c.cpy_id,
        c.id
        from t_cii_area_company c
        left join t_cii_area_area t on t.id = c.id
        <include refid="selectSql"/>
        <if test="orderBy != null">
            ORDER BY ${orderBy}
        </if>
        <if test="order != null">
            ${order}
        </if>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="selectByMapCount" parameterType="java.util.Map" resultType="int">
        SELECT count(c.id) from t_cii_area_company c
        left join t_cii_area_area t on t.id = c.id
        <include refid="selectSql"/>
    </select>

    <!-- 通过指定条件查询信息 -->
    <select id="selectByMapSimple" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
        t.parent_id,
        t.area_name,
        t.short_name,
        t.area_type,
        t.longitude,
        t.latitude,
        t.sort,
        t.id,
        t.remark,
        t.is_deleted,
        c.cpy_id,
        c.id
        from t_cii_area_company c
        left join t_cii_area_area t on t.id = c.id
        <include refid="selectSql"/>
        <if test="orderBy != null">
            ORDER BY ${orderBy}
        </if>
        <if test="order != null">
            ${order}
        </if>
    </select>

    <!-- 通过指定条件查询信息总数，用于分页展示 -->
    <select id="selectByMapSimpleCount" parameterType="java.util.Map" resultType="int">
        SELECT count(c.id) from t_cii_area_company c
        left join t_cii_area_area t on t.id = c.id
        <include refid="selectSql"/>
    </select>
</mapper>